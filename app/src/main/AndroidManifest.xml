<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.calojy.ui2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login" />
        <activity android:name=".myDBClass" />
        <activity android:name=".info1" />
        <activity android:name=".info0" />
        <activity android:name=".info2" />
        <activity android:name=".forgot" />
        <activity android:name=".dash" />
        <activity android:name=".ItemData" />
        <activity
            android:name=".SimpleImageArrayAdapter"
            android:label="@string/title_activity_simple_image_array_adapter"
            android:theme="@style/Theme.AppCompat.NoActionBar"></activity>
    </application>

</manifest>